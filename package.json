{
  "name": "claude-translate",
  "version": "0.3.7",
  "description": "Save tokens on Cyrillic text in Claude Code: cheap Haiku translate tier, lazy EN doc cache via PreToolUse hook, CLAUDE.md sync, MCP",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/davlet42/claude-translate.git"
  },
  "homepage": "https://github.com/davlet42/claude-translate",
  "bin": {
    "claude-translate": "dist/cli.js"
  },
  "files": [
    "dist",
    "plugin",
    "templates"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "node scripts/sync-plugin-version.mjs --check && npm run build",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "npm run build && node --test test/*.test.mjs",
    "version": "node scripts/sync-plugin-version.mjs && git add plugin/.claude-plugin/plugin.json .claude-plugin/marketplace.json"
  },
  "keywords": [
    "claude",
    "claude-code",
    "translation",
    "cyrillic",
    "russian",
    "tokens",
    "mcp",
    "hooks",
    "plugin"
  ],
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@cursor-translate/core": "^0.2.11"
  },
  "optionalDependencies": {
    "@cursor-translate/mcp": "^0.2.7"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
